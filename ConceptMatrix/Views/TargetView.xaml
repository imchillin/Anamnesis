<UserControl x:Class="ConceptMatrix.GUI.Views.TargetView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:ConceptMatrix.GUI.Views"
			 xmlns:fa="http://schemas.fontawesome.io/icons/"
			 mc:Ignorable="d" 
			 d:DesignWidth="250">
	<Button Height="40" Padding="0" HorizontalContentAlignment="Stretch" Click="OnClicked">
		<Grid Margin="3">
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="32"/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>

			<Grid Grid.Column="0" Grid.RowSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center">
				<fa:ImageAwesome Icon="User" Margin="3" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"/>
			</Grid>

			<Label Grid.Column="1" Grid.Row="0" x:Name="ModeLabel" Content="Auto Selection" FontWeight="Light" Margin="3,0,0,0" Padding="0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"/>
			<Label Grid.Column="1" Grid.Row="1" x:Name="NameLabel" Content="Something" FontWeight="Medium" Margin="3,-4,0,0" Padding="0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"/>
		</Grid>
	</Button>
</UserControl>
