<UserControl x:Class="ConceptMatrix.AppearanceModule.Views.ColorControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:ConceptMatrix.AppearanceModule.Views"
			 xmlns:cm3Controls="clr-namespace:ConceptMatrix.WpfStyles.Controls;assembly=ConceptMatrix.WpfStyles"
			 mc:Ignorable="d"
			 Width="53"
			 Margin="1"
			 HorizontalAlignment="Left">
	<Grid x:Name="ContentArea">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="24"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		
		<Button Grid.Column="0" Padding="0" Style="{DynamicResource TransparentButton}" Click="OnClick" Margin="0">
			<Border MinHeight="20" CornerRadius="3" BorderBrush="{DynamicResource MaterialDesignBody}" BorderThickness="1" Margin="2">
				<Border.Background>
					<SolidColorBrush x:Name="PreviewColor" Color="{Binding WpfColor, FallbackValue=White}"/>
				</Border.Background>
			</Border>
		</Button>

		<cm3Controls:NumberBox Grid.Column="1" MinWidth="20" Value="{Binding Value}"/>

	</Grid>
</UserControl>
