<UserControl x:Class="ConceptMatrix.EquipmentModule.Views.ItemView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:ConceptMatrix.EquipmentModule.Views"
			 xmlns:converters="clr-namespace:ConceptMatrix.WpfStyles.Converters;assembly=ConceptMatrix.WpfStyles"
			 mc:Ignorable="d" 
			 d:DesignWidth="350"
			 Margin="0">

	<UserControl.Resources>
		<ResourceDictionary Source="pack://application:,,,/ConceptMatrix.WpfStyles;component/Main.xaml" />
	</UserControl.Resources>

	<Button HorizontalContentAlignment="Stretch" Click="OnClick" Height="75" Width="256" 
			BorderBrush="Transparent"
			BorderThickness="0"
			Style="{DynamicResource MaterialDesignOutlinedButton}">
		<Grid Margin="3">
			
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="35"/>
				<ColumnDefinition Width="1*" />
			</Grid.ColumnDefinitions>

			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<TextBlock Text="{Binding SlotName, FallbackValue='Equipment Slot'}" Grid.ColumnSpan="2" FontWeight="SemiBold"  Foreground="{DynamicResource MaterialDesignBody}"/>

			<Border Grid.Row="1" Grid.RowSpan="2" Background="#444444" CornerRadius="3" Width="32" Height="32">
				<Grid>
					<Image Source="{Binding Value.Icon, Converter={StaticResource IImageConverter}}"/>
					<Image Source="/EquipmentModule;component/Assets/IconBorder.png" Margin="-2, -1, -2, -3"/>
				</Grid>
			</Border>

			<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Value.Name}" Margin="6, 0, 0, 0" FontWeight="DemiBold" Foreground="{DynamicResource MaterialDesignBody}"/>
			<TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Value.MainModelQuad}" Margin="6, 0, 0, 0" FontWeight="Light"  Foreground="{DynamicResource MaterialDesignBody}"/>

			<Rectangle 
				Grid.ColumnSpan="2"
				Grid.Row="1"
				Grid.RowSpan="2" 
				Fill="Transparent" 
				Visibility="{Binding Value.Description, Converter={StaticResource StringHasContentToVisibility}}" 
				ToolTipService.ToolTip="{Binding Description}"/>

		</Grid>
	</Button>
</UserControl>
