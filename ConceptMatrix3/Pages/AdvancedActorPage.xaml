<UserControl x:Class="ConceptMatrix.GUI.Pages.AdvancedActorPage"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:ConceptMatrix.GUI.Pages"
			 mc:Ignorable="d" 
			 d:DesignHeight="600"
			 d:DesignWidth="900"
			 Loaded="OnLoaded">
	
	<Grid HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="ContentArea">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Column="0" Grid.Row="0" Text="Actor Type:" Style="{StaticResource Label}"/>
		<ComboBox Grid.Column="1" Grid.Row="0" x:Name="ActorTypeComboBox" SelectedItem="{Binding ActorType}" Margin="3, -2, 3, 3" MinWidth="100"/>

	</Grid>
</UserControl>
